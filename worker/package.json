{
  "name": "trainboard-worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute trainboard --local --file=./schema.sql",
    "db:migrate:remote": "wrangler d1 execute trainboard --remote --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "jose": "^5.9.0",
    "pbf": "^4.0.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^6.0.3",
    "wrangler": "^3.0.0"
  }
}
